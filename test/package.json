{
	"scripts": {
		"prettier": "prettier --check .",
		"prettier:fix": "prettier --write .",
		"start": "wrangler dev --ip localhost --local-protocol https --port 8788"
	},
	"devDependencies": {
		"wrangler": "^3.1.2"
	}
}
